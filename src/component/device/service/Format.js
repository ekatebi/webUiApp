// Helper functions to format device details.

export function capitalize(inputString) {
  const outputString = inputString && inputString[0].toUpperCase() + inputString.slice(1);
  return outputString;
}
